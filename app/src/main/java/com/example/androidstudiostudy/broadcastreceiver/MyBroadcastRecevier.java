package com.example.androidstudiostudy.broadcastreceiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.widget.Toast;

import com.example.androidstudiostudy.data.UserBean;

/*public class MyBroadcastRecevier extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent != null){
            UserBean userBean = (UserBean) intent.getSerializableExtra("checkView");
            Toast.makeText(context, "广播接收器接收到了"+userBean.getName()+userBean.getPhone(), Toast.LENGTH_SHORT).show();
        }
    }
}*/
